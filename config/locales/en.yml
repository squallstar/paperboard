en:
  activerecord:
    errors:
      models:
        user:
          attributes:
            current_password:
              match: "doesn't match with your user password."

  plans:
    features:
      one: "%{projects} project, %{team} team, %{users} users"
      other: "%{projects} projects, %{team} teams, %{users} users"
    names:
      free: "Free"
      standard: "Start-up"
      enterprise: "Enterprise"